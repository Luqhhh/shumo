# Q4 O3：checkpoint 账本瘦身

2026-09-13，依据用户“继续”推进已给定的O3工程方案。仅改变持久化表示，不改变批准的v3预测、目标、约束、反馈及收费。

## 保存与恢复

checkpoint schema由2升为3，不再重复保存所有历史合同版本和账单。保存时间、SOC、预测快照、母计划、训练状态、配置/源码/输入指纹、控制计数以及六个日志的已提交长度和SHA256；账本只保存case、账单数、合同日期数、最近合同版本。

日志先flush和fsync，再绑定前缀并提交checkpoint。恢复先核对schema、指纹、配置、六个前缀哈希和训练状态，截去未提交尾部，再从contracts和cost_ledger重建历史账本。合同重新走submit检查权限、相邻调整量和版本；账单必须从2月1日连续覆盖到恢复时刻。交易版本的实际价格从相应已结算账单恢复。最后核对账本见证、执行步数和控制计数。恢复阶段另记checkpoint_restore计时。

历史账本仍完整保留于内存和不可变日志，费用接口、年度独立验证及报告不改。schema 1/2 checkpoint不自动迁移或跨源码续跑；旧文件不覆盖。运行选择和AI人工核验保持待确认。

## 年度规模隔离测量

测量源码SHA256：`95ee0ba8e2ca123155453d49eb9a441e7b48cc42536ac20a7cc8e3df871a4d85`，本地冻结于outputs/evidence/q4_persistence_sparse/o3_source.tar。
从两个旧main年度checkpoint与日志重建，各48096条账单；Q4-2合同334个，Q4-3合同1336个。完整重建结果与原checkpoint中的全部合同和账单逐项相同，原文件哈希不变。

| 项目 | Q4-2 | Q4-3 |
|---|---:|---:|
| 完整checkpoint字节 | 11,049,286 | 14,835,519 |
| 瘦身表示字节 | 50,685 | 43,097 |
| 完整写入3次范围，秒 | 0.421–0.457 | 0.631–0.673 |
| 瘦身写入3次范围，秒 | 0.0021–0.0024 | 0.0018–0.0021 |
| 日志重建账本，秒 | 0.406 | 0.755 |

这里计时只有JSON序列化及原子替换，不含日志flush/fsync、前缀哈希、预测初始化、求解、审计及完整恢复流程。不外推全年提速。保留旧运行的原源码身份17c55c0b…；旧schema 1没有原生前缀SHA，本次明确是事后全文哈希与长度绑定，不追写旧checkpoint。

输出的serialization-fixture不是可恢复运行，不以其旧配置/源码元数据冒称schema 3正式产物。数据、对照JSON及源码包仅本地保存。

## 回归

Q4-2连续/恢复结果一致，拒绝旧schema、输入指纹变化、账本计数冲突和同字节数日志篡改，丢弃未提交尾部。Q4-3分别在06/12/18和午夜checkpoint中断，再续跑穿过下一日；预测、合同、求解母计划、执行和账单逐条一致（只去除native计时），费用及状态完全一致，实际交易价格与版本恢复正确。使用合成数据的回归不作为正式年度数值。

只读复测命令，output须为新目录：

```bash
uv run --locked python scripts/benchmark_q4_checkpoint.py --run outputs/runs/q4_2/q4-2-v3-annual-main-001 --output outputs/evidence/q4_persistence_sparse/q4_2-new
```

后续O4独立修改稀疏结构；新预测、风险采购或终端价值仍待用户选择。没有实施新策略全年复算或修改正式Excel。

本地完整检查：267 passed、1 skipped（可选Q1真实MILP）；Ruff、格式、锁文件和AI记录检查通过。
