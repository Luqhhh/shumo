# Q4长窗PV偏差校正：事前模型卡

2026-09-13。参数由Agent依据用户[全方案操作委托](approvals/2026-09-13-q4-optimization-delegation.md)确定，D_OPTIMIZATION_Q4放行；不是声称用户亲自逐项选定参数，也不是改写已批准融合规则。私有事前参数记录位于outputs/evidence/q4_solver_candidates/remaining_strategy_parameters.json，早于本项运行。独立模型q4-v4-pv-bias-long，仅Q4-3/main，CLI `--pv-method bias-long`。默认v3和此前融合保留。

## 固定规则

从2025-02-01初态新跑，只修改原始00/06/12/18发布快照中原始时距严格超过12 h的PV点。历史代理H为现有7日历史PV预测，不加残差校正；12–18、18–24 h分桶，按发布时H>1 kW/≤1 kW分组，不用未来实际PV判昼夜。原始F仍为v3官方预测组合/插值，LOAD和价格不变。

只收集保存过、目标已实现的原始issue-target对，r−28日 < target ≤ r，等权计每个原始发布对；原始F不以先前校正输出替代。每组μ=mean(actual−saved_original_F)，新预测max(0,F+μ)。冷启动r<2025-02-08，短窗≤12 h或空样本组保持F。均值、裁剪、窗口及分桶事前固定，不看全年再扫参。H只用于固定代理分组，历史MAE可作为诊断，无融合权重。

因果provenance保存原始F/H、原始lead、目标、样本数、signed error sum、μ和输出，完整预测哈希绑定全字段；actual经InfoSet和连续历史验证后才到训练器。恢复重放已提交预测前缀和已结束历史，校验训练见证、冻结快照和日志前缀，拒绝跨源码/schema/配置续接。运行/导出共同核对委托快照与精确有效参数。

## 固定边界和评价

保持v3的合同权限、实际限充/负荷限放、费用公式、求解及物理容差、窗口和普通终端值；最后24 h E≥6000和硬实际末态6000不变。该项与融合不叠加，比较保留工程v3 q4-3-v3-engineering-annual-main-001。

先真实冷启动周1008步，再初态连续两周2016步及启用后受控中断/恢复；核对短窗/冷启动和LOAD/价格/终端/原PV来源不变、完整母计划/后缀与因果链。通过后独立连续334日/48096步年度，比较总费与初始/调整/实际紧急费、MAE分时距、余电/弃光/吞吐/保护及末态；完整审计后按原模板副本导出读回。失败和负结果保留，不删除失败轨迹或把费用前缀称全年。

已查看年度属于开发诊断数据；因果回放不冒称未见独立测试集。此卡事前规则与稍后验证结论分别记录。AI人工核验不代填，全局final阻断保留。
