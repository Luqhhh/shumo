# Q4 O4：缓存MILP稀疏结构

2026-09-13。O3独立提交80dd617之后，实施用户给定的O4工程方案。保持变量和约束顺序、显式零系数、目标及整数模式；不替换求解器、不改变容差或模型。

## 结构与数值分离

缓存键为窗口长度、完整合同权限布局和约束结构版本1，LRU最多432项。缓存只含CSC indptr/indices、固定系数、行上下界模板和动态系数位置，数组设为只读。每次问题实例复制数值数组，重新计算load/PV/price、Gmax、当前SOC、合同量、调整权限、年末预留和年度可达边界。不会将上次的预测量、合同量或SOC缓存为新窗口输入。

按原COO→CSC排序规则一次建立结构，每次直接构造CSC实例。仍调用锁定的SciPy/HiGHS MILP，原生求解、残差核验、规范化和独立物理验证不改。performance_summary计入实际结构命中/未命中数，后缀复用不计为建模或求解。

## 矩阵等价

冻结97cc6e9的旧构造器为tests/fixtures/q4_milp_reference.py，仅作为测试对照；原完整源文件SHA256为cc8891b760aef2fe62ae1caa005f5f3155a6d15a39dce0a8fc5b05d09e131bf2。其逐行COO构造独立于新的模板及数值位置更新。

17项新增回归覆盖Q4-2/Q4-3、00/06/12/18和普通固定区间、跨日look-ahead、不同窗口长度、精确零负荷/PV/合同量、不同预测和合同数值、v3年末边界和历史v2口径。逐项比较目标、变量边界、整数位置、约束上下界及CSC矩阵；常规窗口同时比较data/indices/indptr字节。实例更新不能改写缓存或之前的问题矩阵。

另外，将真实一周日志中162/144个全部母求解窗口重新构造：306个问题的全部数组及CSC字节与冻结旧构造器相同，不重新求解或更换策略。证据保存在outputs/evidence/q4_persistence_sparse/actual_sparse_equivalence.json，复核脚本和参考文件哈希一起保存。

## 两条真实一周闭环

从2月1日初态连续执行至2月8日，各1008步，同机器、输入、依赖及BLAS/OMP/MKL单线程环境；原生HiGHS选项不变。

- O3基线源码：95ee0ba8e2ca123155453d49eb9a441e7b48cc42536ac20a7cc8e3df871a4d85。
- O4源码：a11b6f775546c674f7e4041d7bc49ed2c18f9577db1c132fa54dafa45b8b00a5。

| Case | 矩阵构造秒数 | 含审计与指标的运行秒数 | 缓存命中/首次构造 |
|---|---:|---:|---:|
| Q4-2 | 0.412 → 0.269 | 13.873 → 13.562 | 97 / 65 |
| Q4-3 | 0.363 → 0.277 | 14.331 → 14.060 | 86 / 58 |

各完整预测、合同、执行、账单、母计划及求解记录逐条一致（仅去除计时和已声明的证据表示字段），日汇总CSV字节相同。费用保持364258.2094600039 / 371284.07057531655元，实际求解162/144次及复用846/864次相同。

矩阵构造本次下降约35%/24%，完整运行约2%差异仅为一次成对测量，未统计运行波动，不作为稳定端到端或全年加速承诺，不宣称电费降低。包含新schema 3 checkpoint的一周基线也与97cc6e9周轨迹逐条一致，验证O3没有改变闭环结果。年度末态序列化fixture的50.7/43.1 KB不代表所有时点checkpoint大小；周运行峰值约197.6/152.2 KB，取决于当前预测与母计划。

运行和对照保存于outputs/evidence/q4_persistence_sparse/before_o4、after_o4、o4_closed_loop.json和o3_closed_loop_equivalence.json。旧年度77个冻结证据、运行选择、模型批准和Excel均不改；新周运行不作为正式全年交付。

新预测、风险目标、终端策略仍待用户选择，未实施新的策略对照或全年复算。

本地全量284 passed、1 skipped（可选Q1真实MILP）；本次提交范围Ruff/格式、锁文件、AI记录及两份论文草稿编译通过。工作区并行新增的论文绘图脚本有格式问题，本次未改写或纳入提交。77个冻结旧产物哈希复核不变。
