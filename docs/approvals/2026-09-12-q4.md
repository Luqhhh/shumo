# Q4 v2 批准记录

批准日期：2026-09-12（北京时间）。批准人：本会话用户。

用户在收到当前Q4进度及下一步说明后，明确指示：

> 落实 Q4 模型及相关范围的批准记录

本记录将该指令落实为对此前已交付、可审阅的Q4 v2实施规范的批准。
设计基线为提交 `f8b4af0` 的 [q4_implementation_plan.md](../q4_implementation_plan.md)
与 [q4_model_cards.md](../q4_model_cards.md)。本次仅同步批准状态、范围和门槛，
保留v2数学公式、经验参数、控制假设和验收条件。

机器可读记录以 [configs/decisions.toml](../../configs/decisions.toml) 为准；
`confirmed_by="本会话用户"`、`confirmed_at="2026-09-12"` 如实引用此次授权，
不推定用户姓名或其他队员签名。

| Decision | 批准范围 | 规范定位 |
|---|---|---|
| D_MODEL_Q4_2 | 仅q4_2；完整基础MILP、LOAD-A、历史PV与价格预测、合同冻结、实际反馈、发布锚定窗口、终端价值、年度6000端点、求解/缓存/checkpoint和失败规则 | v2第1—7、9—10节；Q4-2卡 |
| D_MODEL_Q4_3 | 仅q4_3；完整基础MILP、LOAD-A与价格预测、官方PV版本组合/重采样、合法逐版调整、实际反馈、窗口、终端/年度及工程失败规则 | v2第1—7、9—10节；Q4-3卡 |
| D_TIME_TEMPLATE_EXPORT_Q4 | q4_2/q4_3；显式列序、费用分项、334日展开、紧急记录、可选费用汇总表、来源映射与读回 | v2第8节 |
| D_EVAL_Q4 | q4_2/q4_3；各自主价格与lag1对照的四条连续轨迹、独立验证、指标、容差与结论边界 | v2第9—10节 |

Q4的LOAD-A批准在各自模型卡中完整收录；原 `D_LOAD_FORECAST` 仍记录Q3范围。
原Q1的 `D_TIME_TEMPLATE_EXPORT` 不改写，Q4导出使用专用decision。
Q4比较使用 `D_EVAL_Q4`；其他case的全局 `D_EVAL` 和Q2模型仍保持原状态。

审批门槛检查Q4各decision的状态、确认字段和精确 `scope_cases`。
运行门槛各自检查完整模型批准，Q4-2不依赖附件3、D_RESAMPLE或Q2模型批准。
导出门槛使用Q4专用映射；final门槛检查Q4导出/比较批准，选中run的导出快照
必须绑定当前Q4映射和范围，不能以Q1快照替代。

本次批准的是实现和验收口径。Q4预测效果、MILP、短期性能、全年轨迹、实际
年度端点与Excel数值读回仍未运行；既有证据仍为 `design_evidence_only`。
本记录不批准具体run选择、数值结果、正式提交或AI使用记录的人工核验。
`selected_runs.toml` 和AI人工核验状态保持原值。

本次门槛验证：`uv run --locked pytest -q` 为196 passed、1 skipped（可选Q1真实
求解器测试）；Ruff检查及格式检查通过。测试覆盖Q4模型的缺失/待批准/确认字段/
范围阻断、Q4专用导出审批和当前映射快照。当前仓库只读核验确认四项Q4批准
均通过gate，两case运行均进入尚未实现的runner并抛出 `ModelNotImplementedError`；
与设计基线逐项比较，Q4以外既有decision未变，final仍被未完成项阻断。
