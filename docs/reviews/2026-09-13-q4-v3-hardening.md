# Q4 v3 审阅问题修复与既有产物复核

审阅基线为 `main@f31a1ffdda26542caf9767cc44438daf90fda4dc`。审阅引用的
241 passed、1 skipped 来自该提交远端 CI；本次修复另在本地运行测试，二者独立。
修复不修改批准记录、v3费用目标、反馈公式、预测参数、合同权限或储能预留规则。

## 修复范围

1. 输入记录先验证时间戳和值，再成对构造数组。空时间戳、额外、重复、乱序或
   缺失记录直接报错；`Q4Inputs`再次检查三条实际序列长度及公共年度时间键。
2. 独立执行验证重算确定性充放电保护、削减量、PV/合同利用、紧急量、SOC和
   保护原因，检查状态及及时响应标志。物理平衡不能代替批准规则的一致性。
3. 选中运行和导出共同检查运行时模型决策、有效配置、导出版本及v3预留快照。
   比较完整批准内容和适用范围，缺失、冲突或版本错配均阻断。
4. 原五个日志以及输入、配置、领域结果、验证和费用摘要纳入产物哈希清单。
   新运行另保存真正求解时的完整`DispatchPlan`；后缀从母计划独立重建并验证，
   每个执行意图均与对应计划首动作核对。预测按可见历史逐步重放，核对完整快照，
   同时检查发布时间、窗口、预测ID、求解ID、合同、复用父节点和移位次数。
5. checkpoint schema升级为2，绑定所有日志已提交前缀的SHA256。恢复先验证
   全部前缀，再截断未提交尾部；同字节数篡改不能通过。增量哈希避免反复扫描
   全部历史日志。旧schema1 checkpoint仍保留，不能跨源码/schema直接续跑。

历史v2按其原模型版本、预测身份和1200下界复核；v3不能通过关闭预留参数绕过
6000下界。默认v3优化模型不变，不把Q3固定24小时窗口或反馈规则引入Q4。

## 回归检查

`tests/test_q4_review_regressions.py`依据审阅中的六个反例编写，另覆盖序列长度、
公共时间键、执行失败状态、削减量、配置/运行时快照冲突和日志同字节数篡改。
`tests/test_q4_engine.py`检查真正中断/恢复过程中的前缀篡改拒绝，并通过真实
短期MILP生成的日志检查预测、求解ID、意图和绝对界篡改。合成测试不作正式结果。

本地完整测试：258 passed、1 skipped（可选Q1真实MILP），Ruff和格式检查通过。
这不是对原远端CI日志的转述。完整验证器源码/配置快照SHA256为
`0cc91c6f56e854b0cc3e98769d84c5db6d9c46a6716de25566d2c2ff5d2c860f`，
逐文件清单及可运行源码保存在`outputs/evidence/q4_review_verifier_final3/`。

## 既有年度证据处理

对实际附件重新执行严格读取：负载、PV、价格各52560条，与同一年度右端点
时间网格一一对应，无空时间戳或额外记录。四条v3旧轨迹各48096步，补强后的
实际反馈、物理、权限、费用和末态验证全部通过；未发现需要改写费用或重跑
执行轨迹的输入错位、动作偏差。

四条控制链最终复核均通过：每条1336个预测快照、48096个执行意图；合计重建
44080次母解并核对148304次后缀复用。两个主方案的选中运行证据检查无阻断。
四条未舍入年度费用与原摘要之差均不超过0.01元，原文件哈希全部保持一致。
最终清单位于`outputs/evidence/q4_review/completion.json`，运行选择仍待人工确认。

旧求解日志未保存完整母计划，因此补充证据不能仅由旧摘要生成。审计程序在
旧预测、实际状态及合同上下文重建真正求解的母计划，逐步核对原动作意图及
后缀证书；它验证既有执行轨迹，不执行另一条年度策略。重建计划单独保存，
明确标记为事后验证材料，不能冒称原运行已记录的计划。

补充清单及新验证报告位于
`outputs/evidence/q4_review/<case>/<run_id>/`。清单绑定旧运行文件、原运行时
manifest、重建计划及新反馈验证报告的哈希。旧run、Excel、执行日志、源码快照
均不修改；具体运行选择和AI人工核验不由本次工程验证自动批准。
补充清单只有在旧文件与验证源码保持稳定后发布，并绑定最终审计报告的哈希；
选中运行检查也要求报告成功、旧文件未变、验证源码稳定，拒绝未完成的清单。

复现命令（审计目录必须尚不存在）：

```bash
uv run --locked pytest -q tests/test_q4_review_regressions.py
uv run --locked python scripts/audit_q4_run.py --case q4_2 --run-id q4-2-v3-annual-main-001
```

若已有独立重建的计划，可通过`--verified-plans <path>`使用当前验证器完整重查，
避免重复调用MILP；仍重放预测、核对每步意图、独立验证物理费用并重新绑定哈希。
完整逐格数据和补充审计文件留在本地，Git交付只包含代码、测试及工程说明。
