# Q4固定变量消元：事前工程试验

2026-09-13。Agent依据D_OPTIMIZATION_Q4和用户全方案委托选择有限且lower==upper的变量，在真正求解前消元。原始目标系数、全矩阵、行/变量边界、整数模式及物理验证保留。重建完整x后由原验证器检查供需、SOC、合同、互斥、预留与目标/下界证书，不删DispatchPlan字段。若固定量的目标偏移非零，回退原完整MILP，保持原relative-gap停止口径；无自由变量也回退，不伪造成功。

CLI `--solver-method eliminate-fixed`，模型版本仍q4-v3-reserve，effective_config另存精确工程方法/参数，运行/导出绑定D_OPTIMIZATION_Q4快照，模拟前封装源码。仅允许v3/main，不与PV或风险新策略混用；求解器/容差保持原样。旧run和v3默认仍按原记录核验。

先用两条已核验工程年度真实母窗口的确定性410窗口样本做交替顺序成对基准，独立重建/物理检查每解，对照目标及accepted absolute gap envelope。再从初态真实连续Q4-2/Q4-3各两周，核对完整预测/合同权限/执行和费用；首动作变化不冒称闭环费用不变。共享机器微小耗时差不作为稳定加速证据，没有明确收益保留试验而不启用默认。此项先工程诊断，不将两周费差写成全年。
